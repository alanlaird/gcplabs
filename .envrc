# Build KUBECONFIG from whichever project kubeconfigs exist
_kc=""
for _f in \
  "$PWD/spot-kind/kubeconfig.yaml" \
  "$PWD/spot-kind-submariner/kubeconfig.yaml"
do
  [ -f "$_f" ] && _kc="${_kc:+$_kc:}$_f"
done
[ -n "$_kc" ] && export KUBECONFIG="$_kc"
unset _kc _f

